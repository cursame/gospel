<div class="menu-bar">
	<ul class="menu">
    <li><%= link_to image_tag('main_logo.png'), root_path%></li>
		<% if session[:type] != nil %>
	  <% if session[:type] == 'network' %>
		<li><%= link_to 'Institución', institution_path  %></li>
		<% end %>
		<% if session[:type_user] == 'admin' %>
		<li><%= link_to 'Admin', admin_path %></li>
		<%end%>
		<% if session[:type_user] == 'teacher' %>
		<li><%= link_to 'Profesor', teacher_path %></li>
		<%end%>
		<% if session[:type_user] == 'student' %>
		<li><%= link_to 'Alumno', student_path  %></li>
		<%end%>
	    <li><%= link_to 'Salir', exit_path  %></li>
		<%else%>
	    <li>
	    <% image_tag ('i_user.png') %><%= link_to 'Soy usuario', '/trainer/follow_user' %></li>
	    <li><%= link_to 'Soy institución', '/trainer/follow_network' %></li>
	    <li><%= link_to 'Gaceta de Cúrsame', newsletter_path %></li>
		<%end%>
	</ul>
</div>
